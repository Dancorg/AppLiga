{
  "name": "nodetest",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "build": "npm run build --prefix frontend/app-liga-front",
    "start": "node backend/src/index.js",
    "deploy": "npm run build && npm run start"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.5.0",
    "wait-on": "^9.0.5"
  }
}
